"""
{PROBLEM_TITLE}

{PROBLEM_DESCRIPTION}

Constraints / notes:
{CONSTRAINTS}
"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from utils import *  # TreeNode, build_tree, build_graph, print_grid, etc.
from drill.harness import run


def solution():
    """YOUR CODE HERE"""
    pass


# ---------------------------------------------------------------------------
# Test cases: (args, expected). args can be a single value or a tuple.
# Write your edge cases BEFORE you code. That's the drill.
# ---------------------------------------------------------------------------
CASES = [
    # (input_args, expected_output),
]


if __name__ == "__main__":
    run(solution, CASES)
